import * as sinon from 'sinon';
import * as fs from 'fs';
import { expect } from 'chai';


describe("Settings", () => {

    it("checks that settings contains the only init function", () => {
        expect(1).to.equal(1);
       // expect({}).to.have.property("init");
    });
});